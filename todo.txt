1) Instances of items for inventory: need this for bind on equip/bind on pickup
     new db table 'inventory', save items in same style as items db
     store reference to item db... with instance data as needed

"learn" if skill known to multiple classes,
make sure trainer is valid

use "base" stats before equipment for regen


combat rounds with weapon attack speed for # attacks

move checks for enter/exit room scripts out of changerooms function

equipping two weapons for new char (rogue) bugged